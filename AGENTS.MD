### ---- Personal organizer regarding events, tasks and reminders, including morning digest about planned events. ---- ###

## 1. Логика экранов и кнопок ##

Базовые принципы

В каждом состоянии есть «локальные» действия и обязательная пара навигации:
⬅️ Назад (к предыдущему экрану) и 🏠 Меню (к корню).

На «корневом» экране нет «Назад» (некуда назад), зато есть главные действия.

Любое действие, требующее «мастер-режима» (изменение времени, админка), не подменяет дайджест на экране — редактируется текст этого экрана с контекстной клавиатурой.

Минимизируем «лишние» сообщения: если экран уже показан — редактируем его, а не присылаем новый.

Для гостей и неадминов скрываем кнопки, которые им недоступны.

Карта состояний → кнопки

Главный экран (дайджест, “корень”)
Кнопки:

↻ Обновить

➕ Добавить напоминание

✏️ Мои напоминания

⚙️ Настройки (только для админа)
(Нет «Назад», есть «корневые» действия.)

Настройки (root настроек)
Кнопки:

⏰ Время дайджеста

[адм.] (только для админа)

⬅️ Назад (в «Меню»)

🏠 Меню

Редактор времени дайджеста
Кнопки:

−10 мин | +10 мин

−1 час | +1 час

✅ Сохранить

⬅️ Назад (в «Настройки»)

🏠 Меню

Добавление напоминания (экран-подсказка, ожидание текста)
Кнопки:

ℹ️ Формат даты (покажем подсказку о DD-MM-YYYY)

⬅️ Назад (в «Меню»)

🏠 Меню
(После получения текста возвращаем на «Меню» и показываем обновлённый дайджест.)

Список моих напоминаний (выбор элемента)
Кнопки:

(по одному на строку) Название напоминания → открывает экран элемента

➕ Добавить

⬅️ Назад (в «Меню»)

🏠 Меню

Карточка выбранного напоминания (просмотр/редактирование)
Текст: выбранный пункт + краткая инструкция «отправь новый текст (и дату) одним сообщением».
Кнопки:

🗑 Удалить

👥 Поделиться / 🙈 Только мне (тумблер видимости; для админа — управляет «share»; для гостя всегда «Поделиться» по умолчанию)

⬅️ Назад (к списку)

🏠 Меню

После удаления / изменения напоминания (статус-квиток)
Кнопки:

↩️ К списку

🏠 Меню

Нет дайджеста в кэше / ошибка загрузки
Текст: подсказка «Нажми ↻ Обновить».
Кнопки:

↻ Обновить

🏠 Меню

Неавторизованный пользователь
Текст: «❌ Вы не авторизованы…»
Кнопки:

🏠 Меню (попытка вернуться в корень всё равно снова покажет отказ; можно добавить кнопку «📞 Связаться с админом» с ссылкой/юзернеймом)

Гостевой дайджест (для гостя или теста админом себе)
Кнопки:

↻ Обновить (пересобрать гостевой текст)

🏠 Меню
(Гостю не показываем «Настройки», «Мои напоминания» оставляем, если вы хотите чтобы гость видел/редактировал только свои.)

Единые UX-правила

Всегда держим «⬅️ Назад» и «🏠 Меню» на последней строке клавиатуры, чтобы мышечная память работала одинаково.

На экранах-редакторах обязательно есть «✅ Сохранить» (если требуется явное подтверждение) либо явная инструкция «отправь текст и вернёшься в меню».

При переходах корректируем флаги состояния (например, at_root=True только на корневом экране), чтобы не всплывали лишние копии дайджеста.

Админ видит больше: «⚙️ Настройки», «[адм.]», тумблер «Поделиться» для своих напоминаний; гость — упрощённый набор.